/* export class Router {
  routes: { path: string, name: string }[] = [
    {
      path: '/',
      name: 'Root'
    },
    {
      path: '/about',
      name: 'About'
    },
    {
      path: '/scores',
      name: 'Scores'
    },
    {
      path: '/game',
      name: 'Game'
    }
  ];

  pathname: string = window.location.pathname;

  navigate = (e) => {
    
  }
} */